    <joint name="joint_{{ joint_num }}" type="prismatic">
        <parent link="{{ par_name }}"/>
        <child link="{{ child_name }}"/>
        <origin xyz="{{ origin }}" rpy="0 0 0" />
        <limit effort="1000.0" lower="-0.1" upper="0.1" velocity="0.5"/>
        <axis xyz="{{ axis }}" />
    </joint>